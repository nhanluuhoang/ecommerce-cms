import React from 'react';
import { useTitle } from '../../../hooks/useTitle';

export const Register = () => {
    
    useTitle('Register')
    
    return (
        <div>
            register
        </div>
    );
};
